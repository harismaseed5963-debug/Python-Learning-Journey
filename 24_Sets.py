# Inside the set the repitative value is print only one time 
# In below example the 5 is two time in the set and it is printed only one time 
my_set = {5, 5, 6, 7} # We only kept Values within the set.
print(my_set) 
# How to Create an empty Set?
Empty_Set = set() # Don't Use {} For Creating empty set
print(type(Empty_Set))
